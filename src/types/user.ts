export type User = {
    id: string;
    name: string;
    email: string;
    password: string;
    image: string;
    isAdmin: boolean;
    status: string;
    resetToken: string;
    confirmationCode: string;
    createdAt: string;
    updatedAt: string;
}
