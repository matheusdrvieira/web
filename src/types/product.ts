export type Product = {
    id: string;
    name: string;
    description: string;
    code: string;
    sku: string;
    isActive: boolean;
    image: string;
    price: number;
    createdAt: string;
    updatedAt: string;
}
